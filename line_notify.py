import requests
import sys
import os

def notify():
    # url = "https://notify-api.line.me/api/notify"
    # token = 'nYa6r5UATOhpyOCQ2ksQ7tuDROylZuESiJc4zX09J2d'
    # headers = {"Authorization" : "Bearer "+ token}

    # args = sys.argv
    # if len(args) > 1:
    #     title = args[1]
    # else:
    #     title = "program"
    # message = "\n" + title + " finished."
    # payload = {"message" :  message}

    # r = requests.post(url ,headers = headers ,params=payload)
    os.system("echo c@rl0s112358 | sudo shutdown -h now")

if __name__ == '__main__':
    notify()
